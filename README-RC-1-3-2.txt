Fire-S RC 1.3.2 - Management KPI Count/Filter Sync

Upload:
- app.js

Fix:
- Management KPI cards now calculate counts using the exact same gateway filters they open.
- Premises Requiring Action count matches its filter.
- Overdue Inspections count matches its filter.
- Compliant Sites count matches its filter.
- Inspections This Month now applies the month gateway filter directly.
