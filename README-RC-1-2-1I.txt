Fire-S RC 1.2.1I - Mission Control Gateway Stability Fix

Upload:
- styles.css

Fix:
- Inspection Gateway card no longer flashes blue then ends red.
- The card now keeps one stable blue gradient state across Home render, role-lock and Back to Home.
- Hover/focus transform disabled inside Mission Control to reduce frontpage hop.
- Mission Control gets a minimum layout area to reduce initial reflow.
