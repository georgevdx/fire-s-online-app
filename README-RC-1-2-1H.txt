Fire-S RC 1.2.1H - Status Count + CSS Dedupe

Upload only:
- app.js
- styles.css
- README-RC-1-2-1H.txt

Changes:
- Status dropdown counts now use the same filter matcher as the visible Projects list.
- Fixes Previous Cycles count mismatch between dropdown count and card/list results.
- CSS duplicate selector blocks were cleaned conservatively, keeping the last occurrence of duplicate selectors.
- No new filter logic or UI modules added.
